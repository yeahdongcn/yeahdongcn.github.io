# night-city-pixel

Read **AGENTS.md** for the project map, hard invariants, and conventions — it is the
single source of truth for working in this repo.

Quick reference:
- Test gate: `node test/smoke.js` (run 2-3×, must stay green)
- Serve: `./run.sh` → http://localhost:8080
- Ship checklist: smoke green → bump `?v=N` in index.html → update README if player-facing
